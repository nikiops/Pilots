# 📊 Структура БД TgWork

```
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE SCHEMA                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│      USERS          │
├─────────────────────┤
│ id (PK)             │
│ telegram_id (UNIQUE)│
│ first_name          │
│ last_name           │
│ avatar_url          │
│ bio                 │
│ skills              │
│ rating (0-5)        │
│ total_reviews       │
│ balance             │
│ total_earned        │
│ total_spent         │
│ completed_orders    │
│ cancelled_orders    │
│ is_active           │
│ is_banned           │
│ created_at          │
│ updated_at          │
│ last_active         │
└─────────────────────┘
         │
         ├─────────┬──────────┬────────┬─────────┐
         │         │          │        │         │
         ↓         ↓          ↓        ↓         ↓

┌──────────────────────┐  ┌──────────────┐  ┌──────────────┐
│    SERVICES          │  │   REVIEWS    │  │  MESSAGES    │
├──────────────────────┤  ├──────────────┤  ├──────────────┤
│ id (PK)              │  │ id (PK)      │  │ id (PK)      │
│ seller_id (FK)       │  │ order_id (FK)│  │ order_id (FK)│
│ title                │  │ reviewer_id  │  │ author_id    │
│ description          │  │ reviewed_user│  │ text         │
│ category             │  │ rating (1-5) │  │ attachments  │
│ tags                 │  │ text         │  │ is_edited    │
│ price                │  │ created_at   │  │ is_deleted   │
│ execution_days       │  └──────────────┘  │ created_at   │
│ revision_count       │                    │ edited_at    │
│ preview_url          │                    └──────────────┘
│ status               │
│ total_orders         │   ┌──────────────────────┐
│ average_rating       │   │    ORDERS            │
│ created_at           │   ├──────────────────────┤
│ updated_at           │   │ id (PK)              │
└──────────────────────┘   │ buyer_id (FK)        │
         │                 │ seller_id (FK)       │
         │                 │ service_id (FK)      │
         └────────────────→│ price                │
                           │ platform_fee_percent │
                           │ seller_gets          │
                           │ status               │
                           │ is_paid              │
                           │ payment_date         │
                           │ deadline             │
                           │ revisions_used       │
                           │ revisions_allowed    │
                           │ buyer_comment        │
                           │ seller_result        │
                           │ created_at           │
                           │ completed_at         │
                           │ updated_at           │
                           └──────────────────────┘
                                    │
                                    ↓
                           ┌──────────────────────┐
                           │   TRANSACTIONS       │
                           ├──────────────────────┤
                           │ id (PK)              │
                           │ user_id (FK)         │
                           │ order_id (FK)        │
                           │ type                 │
                           │ amount               │
                           │ status               │
                           │ description          │
                           │ reference            │
                           │ created_at           │
                           │ completed_at         │
                           └──────────────────────┘
```

## 📝 Описание таблиц

### USERS
- Основная таблица пользователей
- Может быть одновременно заказчиком и исполнителем
- Хранит рейтинг, баланс, статистику

### SERVICES (Услуги/Кворки)
- Услуги, опубликованные фрилансерами
- Фиксированная цена
- Статусы: PENDING, ACTIVE, REJECTED, HIDDEN

### ORDERS (Заказы)
- Связь между заказчиком и услугой
- Эскроу система для безопасных платежей
- Статусы: WAITING_PAYMENT, IN_PROGRESS, UNDER_REVIEW, COMPLETED, CANCELLED, DISPUTE

### MESSAGES (Чат)
- Сообщения между заказчиком и исполнителем
- Поддержка вложений
- История всех общений

### REVIEWS (Отзывы)
- Оценки и отзывы (только после завершения заказа)
- Влияют на рейтинг пользователя
- Рейтинг от 1 до 5 звёзд

### TRANSACTIONS (Финансы)
- Отслеживание всех финансовых операций
- Эскроу, выплаты, пополнения, возвраты
- Обеспечивает прозрачность финансов

## 🔄 Связи между таблицами

```
Users ←→ Services (один ко многим)
Users ←→ Orders (один ко многим - как buyer)
Users ←→ Orders (один ко многим - как seller)
Orders ←→ Messages (один ко многим)
Orders ←→ Reviews (один к одному)
Orders ←→ Transactions (один ко многим)
Services ←→ Orders (один ко многим)
Users ←→ Reviews (один ко многим - как reviewer)
Users ←→ Reviews (один ко многим - как reviewed_user)
```
